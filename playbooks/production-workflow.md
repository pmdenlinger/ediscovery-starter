# Production Workflow

- Build set (saved search or tag).
- Apply redactions; ensure reason codes.
- Image/TXT/native export; generate DAT/OPT (relative paths).
- Validate in sandbox; check Bates and family integrity.
- Create checksums; encrypt; deliver + cover letter.
